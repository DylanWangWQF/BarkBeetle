# Proof-of-Concept

Run the simulation code:
```
make -j8
./inf
```

In `benchmark`, the script `generate_trees.py` can be used to generate various customized Emlearn-style trees by changing either tree depth or number of features (duplicate).


